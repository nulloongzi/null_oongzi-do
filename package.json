{
  "name": "nulloongzido-tests",
  "version": "1.0.0",
  "private": true,
  "description": "누룽지도 보안 검증 자동화 테스트 (rules + dom-utils)",
  "scripts": {
    "test": "node --test tests/dom-utils.test.js tests/firestore-rules.test.js tests/storage-rules.test.js",
    "test:dom": "node --test tests/dom-utils.test.js",
    "test:rules": "node --test tests/firestore-rules.test.js tests/storage-rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase-tools": "^14.0.0"
  }
}
